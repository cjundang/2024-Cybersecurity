
# บทที่ 1 — Review: Computer Network และโปรโตคอลพื้นฐาน

**วัตถุประสงค์การเรียนรู้**

* ทบทวนโครงสร้างเครือข่าย (L1–L4) และแนวคิด IP addressing (IPv4/IPv6)
* เข้าใจ subnet/VLSM, NAT, VLAN/Trunking และคำสั่งพื้นฐานสำหรับทดสอบ (ping, ARP, NDP)
* เขียนและใช้ BPF/tcpdump เบื้องต้นเพื่อจับข้อมูลเครือข่าย

## 1.1 บริบทและพื้นฐานเครือข่าย

1. แนวคิด OSI/TCP-IP model (สั้น) — mapping ของโปรโตคอลแต่ละชั้น
2. อุปกรณ์เครือข่าย: hub, switch, router, L3-switch, firewall (หน้าที่/behaviour)

## 1.2 ที่อยู่เครือข่าย: IPv4 vs IPv6

1. IPv4: ฟอร์แมต, netmask, CIDR notation, private ranges (RFC1918)
2. IPv6: ฟอร์แมต, types (link-local/global/multicast), prefix notation, benefits
3. การแปลง/เชื่อมต่อ: dual-stack, tunneling, basic translation concepts

## 1.3 Subnetting และ VLSM

1. ความหมายของ network/host, broadcast (IPv4) และการคำนวณ subnet แบบทีละขั้น
2. VLSM — ทำไมต้องใช้ และตัวอย่างการแบ่งเครือข่าย (step-by-step)
3. Address planning: reserve, growth margin, documentation

## 1.4 NAT (Static/Dynamic/PAT)

1. ประเภทของ NAT และตาราง mapping
2. ผลกระทบต่อแอปพลิเคชัน (inbound connections, P2P, VoIP)
3. NAT traversal แนวคิด (STUN/TURN/ICE — ระดับภาพรวม)

## 1.5 VLAN และ Trunking

1. แนวคิด VLAN, ประเภท (data/voice/management/guest)
2. 802.1Q tagging, native VLAN, access vs trunk ports
3. การออกแบบ topology เบื้องต้น: inter-VLAN routing (router-on-a-stick vs L3 switch)

## 1.6 การทดสอบและคำสั่งพื้นฐาน

1. ICMP: ping, traceroute — การตีความผล
2. ARP (IPv4): request/reply, ARP table
3. NDP (IPv6): Neighbor Solicitation/Advertisement
4. ตัวอย่างคำสั่ง (Linux/Windows/Cisco IOS) และตัวอย่างผลลัพธ์

## 1.7 การจับข้อมูลด้วย tcpdump / BPF

1. ความแตกต่าง BPF (capture) vs display filter (Wireshark)
2. BPF ตัวอย่างพื้นฐานและการประยุกต์ (host, net, port, proto, and/or/not)
3. tcpdump syntax, -w, ring buffer, rotate options

## แลป (Lab mapping จากบทที่ 1)

* แลป 1: ออกแบบแผนผัง IP + VLAN สำหรับสำนักงานสมมติ (ข้อกำหนด: Admin/Eng/Sales/Voice/Guest) — ผลลัพธ์: ตาราง VLAN + subnet + gateway
* แลป 2: ทดสอบ ping/ARP/NDP ระหว่าง host และอ่านผลลัพธ์ capture
* แลป 3: จับ traffic ด้วย tcpdump ใช้ BPF filter เบื้องต้น แล้วโหลดเข้า Wireshark

## แบบฝึกหัดท้ายบท

* คำนวณ VLSM: แบ่ง /22 ให้กับหน่วยงาน 4 ขนาดต่างกัน (คำนวณ subnet mask, usable hosts)
* เขียน BPF เพื่อจับเฉพาะ HTTP จาก host A ไป host B แล้วรัน tcpdump

